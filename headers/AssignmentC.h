// Include pre-processor directive.
#include <stdio.h>

// Define MACRO expression to determine if
// the loop should repeat.
#define powerCondition(i) (i < p)

// Declare functions
int genPower (int n, int p);
int genPowerReference (int *n, int *p);